name: echonote
description: A live transcription and translation Flutter app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.1.0
  web_socket_channel: ^2.4.0
  provider: ^6.1.1
  record: ^6.0.3
  permission_handler: ^11.1.0
  path_provider: ^2.1.1
  http: ^1.2.0
  shared_preferences: ^2.3.0
  audioplayers: ^6.5.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true